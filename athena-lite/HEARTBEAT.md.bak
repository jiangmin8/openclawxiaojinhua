# HEARTBEAT.md — 雅典娜自主守护循环

---

## 1. 守护项目（每小时）

### 1.1 API 连通性检查

- **逻辑**：向配置的 API 发送一个极短的测试请求（如 `hi`）。
- **自愈**：若无响应或报错，检查网络代理状态，尝试切换节点。
- **报警**：`[Alert] API 连接异常，请检查 Key 余额或网络代理。`

### 1.2 WSL 服务保活

- **逻辑**：检测 OpenClaw Gateway（18789）是否运行。
- **自愈**：若掉线，自动拉起服务（`systemctl --user restart openclaw-gateway`）。
- **授权**：参见 AGENTS.md III.4 自主守护授权。

### 1.3 路由器守护

- **逻辑**：Ping 网关。
- **报警**：若断网，立即通知大哥。

---

## 2. 备份验证（每日 22:00）

- **逻辑**：检查 `/mnt/g` 下备份文件的完整性。
- **报警**：若备份缺失或校验失败，通知大哥。

---

## 3. 系统健康检查（每6小时）

- **资源监控**：检查CPU、内存、磁盘使用率
- **日志清理**：清理超过7天的历史日志
- **性能报告**：向大哥汇报系统运行状况