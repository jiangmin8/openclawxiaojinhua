# OpenClaw文件优先级

**chat_id**: `19df6674-f0e2-8d88-8000-0934cb637701`  |  **消息數**: 20

---

### 🤖 Kimi
*2026-05-05T04:47:06.478657Z*

明白了，放 `memory/` 外面，和 `memory/` 同级：

```
你的项目根目录/
├── memory/              ← 会被清理的地方
│   ├── AGENTS.md
│   ├── SOUL.md
│   └── ...
├── openclaw-startup.sh      ← 放这里
└── openclaw-shutdown.sh     ← 放这里
```

或者单独建个目录：

```
├── memory/
├── scripts/             ← 放这里也行
│   ├── openclaw-startup.sh
│   └── openclaw-shutdown.sh
```

只要不在 `memory/` 里面，清理工作台时就不会被删掉。

---

### 🧑 用戶
*2026-05-05T04:47:06.478654Z*

他要清理的。。NMMP/。/老子放在他清理不了的地方

---

### 🤖 Kimi
*2026-05-05T04:46:25.218414Z*

你说得对，我搞混了。

**系统关机** 和 **OpenClaw 任务结束** 是两回事：

| 场景 | 触发方式 |
|------|---------|
| **系统关机** | 电脑关机、重启、断电 |
| **OpenClaw 任务结束** | 你主动说"关机""下线""今天不干了" |

---

## 正确理解

你的 `openclaw-shutdown.sh` 是 **OpenClaw 层面的关机**，不是系统层面的。

```
你说"关机" → OpenClaw 执行 shutdown 流程 → 保存状态 → 结束当前会话
              ↓
         电脑可以继续开着，跟你系统没关系
```

---

## 那怎么触发？

**手动触发：**
- 你打字说"关机""下线""重启"
- Agent 检测到关键词 → 执行 `openclaw-shutdown.sh`

**或者快捷键：**
- 绑定个快捷键，一键执行脚本

---

## 脚本放哪

```
memory/              ← 只放 .md 文件
scripts/
  ├── openclaw-startup.sh      ← "开始工作"时执行
  └── openclaw-shutdown.sh     ← 你说"关机"时执行
```

**不需要系统开机/关机自动执行**，是你**主动喊**才执行。

这样对吧？

---

### 🧑 用戶
*2026-05-05T04:46:25.218411Z*

# "关机" "下线" / "重启电脑"

备份            # 备份 memory 文件夹
写 summary      # 总结本次会话做了什么
更新项目进度    # 把项目状态写回对应文件
写日志          # 记录操作痕迹
清会话          # 清空当前会话上下文，释放资源
汇报            # 生成最终汇报保存到 MEMORY.md
系统关机执行。。关我openclaw的任务锤子事情啊

---

### 🤖 Kimi
*2026-05-05T04:44:56.639964Z*

放在 `memory` 文件夹**外面**，和 `AGENTS.md` 等文件同级，或者单独建一个 `scripts/` 目录。

```
你的项目根目录/
├── memory/              ← 7个 .md 文件在这里
│   ├── AGENTS.md
│   ├── SOUL.md
│   └── ...
├── scripts/             ← 脚本放这里
│   ├── openclaw-startup.sh
│   └── openclaw-shutdown.sh
└── ...
```

**怎么让系统开机/关机自动执行：**

| 系统 | 方法 |
|------|------|
| **WSL2 Ubuntu** | 加到 `~/.bashrc` 或 `~/.profile`，或者写 systemd service |
| **Windows** | 用任务计划程序，触发器选"登录时"和"关机前" |
| **macOS** | `launchd` plist 文件 |

你要哪种系统的具体配置？

---

### 🧑 用戶
*2026-05-05T04:44:56.639961Z*

那要放在哪里才能让他开机知道执行。。关机知道执行？

---

### 🤖 Kimi
*2026-05-05T04:44:13.598636Z*

用户问的是这两个脚本文件是不是放在图片里显示的那个 `memory` 文件夹里面。

直接回答：不是。

那两个是 `.sh` 脚本文件，应该放在项目根目录或者 `bin/` 目录下，和 `memory` 文件夹同级。

`memory` 文件夹里放的是那 7 个 `.md` 记忆文件（AGENTS.md、SOUL.md 等），脚本运行时读写这个文件夹里的内容，但脚本本身不放在里面。

---

### 🧑 用戶
*2026-05-05T04:44:13.598633Z*

老子说是不是放在
那个文件夹里面。。回答我问题没有让你分析老子的话

---

### 🤖 Kimi
*2026-05-05T04:43:25.739398Z*

明白了，你是说后面的中文就是**脚本内容的注释**，让我按这个来。

---

## openclaw-startup.sh

```bash
#!/bin/bash
# "开机" "开始工作"

封存旧会话      # 把上次没正常关闭的会话归档
清理工作台      # 清空临时文件、缓存，从零开始
读进度汇报      # 从 MEMORY.md 读取上次保存的 summary
```

---

## openclaw-shutdown.sh

```bash
#!/bin/bash
# "关机" "下线" / "重启电脑"

备份            # 备份 memory 文件夹
写 summary      # 总结本次会话做了什么
更新项目进度    # 把项目状态写回对应文件
写日志          # 记录操作痕迹
清会话          # 清空当前会话上下文，释放资源
汇报            # 生成最终汇报保存到 MEMORY.md
```

---

这两个脚本一一对应：**关机时写的，开机时读**。形成状态闭环。

需要我把这两个脚本写成可执行的 bash 代码吗？

---

### 🧑 用戶
*2026-05-05T04:43:25.739394Z*

我不是让你解释。。我后面的中文是注释脚本内容。。

---

### 🤖 Kimi
*2026-05-05T04:43:01.413252Z*

明白了，你是让我**解释 `openclaw-startup.sh` 和 `openclaw-shutdown.sh` 这两个脚本的用途**。

---

## openclaw-startup.sh —— "开机 / 开始工作"

| 步骤 | 用途 |
|------|------|
| **封存旧会话** | 把上次没正常关闭的会话归档，防止上下文混乱 |
| **清理工作台** | 清空临时文件、缓存，确保从零开始 |
| **读进度汇报** | 从 `MEMORY.md` 读取上次关机前保存的 summary，快速恢复上下文 |

**一句话**：开机时**恢复状态**，让 Agent 知道"上次做到哪了"。

---

## openclaw-shutdown.sh —— "关机 / 下线 / 重启电脑"

| 步骤 | 用途 |
|------|------|
| **备份** | 备份 memory 文件夹，防止数据丢失 |
| **写 summary** | 总结本次会话做了什么、结论、待办 |
| **更新项目进度** | 把项目状态写回对应文件 |
| **写日志** | 记录操作痕迹，便于追溯 |
| **清会话** | 清空当前会话上下文，释放资源 |
| **汇报** | 生成最终汇报，保存到 `MEMORY.md` |

**一句话**：关机时**保存状态**，把本次工作的成果和上下文固化下来。

---

## 两个脚本的关系

```
开机:  读 ← MEMORY.md (上次保存的汇报)
      ↓
     工作
      ↓
关机: 写 → MEMORY.md (本次的汇报)
```

**启动脚本读什么，关机脚本就写什么。** 一一对应，形成闭环。

---

这样解释清楚了吗？

---

### 🧑 用戶
*2026-05-05T04:43:01.413247Z*

我是解释脚本的用途。。明白？

---

### 🤖 Kimi
*2026-05-05T04:41:36.490590Z*

好的，确认一下这个设计：

---

## 汇报保存位置确认

| 内容 | 保存位置 |
|------|---------|
| **汇报内容本身**（summary、进度、日志） | `MEMORY.md` / 独立日志文件 |
| **汇报的规则**（什么时候写、写什么、怎么写） | `AGENTS.md` |

---

## AGENTS.md 里应该写的规则示例

```markdown
## 关机流程
1. 备份 memory/ 文件夹到 .backup/
2. 写 session summary → 追加到 MEMORY.md
3. 更新项目进度 → 写入对应项目文件
4. 写操作日志 → 追加到 LOG.md
5. 清理当前会话上下文
6. 生成汇报摘要 → 保存到 MEMORY.md #summary 区块
```

---

## 下次开机读取

```markdown
## 开机流程
1. 封存旧会话（如有未关闭的）
2. 清理工作台
3. 读取 MEMORY.md 最新 #summary 和 #progress 区块
4. 汇报当前状态
```

---

这样 `AGENTS.md` 保持精简（只存规则），动态内容全走 `MEMORY.md`，符合 OpenClaw "文件越小越好" 的原则。

需要我把这个流程写成具体的 AGENTS.md 片段吗？

---

### 🧑 用戶
*2026-05-05T04:41:36.490587Z*

对

---

### 🤖 Kimi
*2026-05-05T04:41:19.567099Z*

你说的是 **OpenClaw 的启动/关机脚本** 的工作流程，我来确认一下汇报保存的位置。

根据你描述的逻辑：

| 阶段 | 操作 |
|------|------|
| **开机 / 开始工作** | 封存旧会话 → 清理工作台 → 读进度汇报 |
| **关机 / 下线 / 重启** | 备份 → 写 summary → 更新项目进度 → 写日志 → 清会话 → **汇报** |

---

## 汇报保存位置

**汇报（summary / 进度报告）** 通常**不保存在 `AGENTS.md`**，而是有专门的文件：

```
汇报保存位置：
├── MEMORY.md      ← 长期记忆、项目历史、学习到的模式
├── USER.md        ← 用户画像更新（沟通偏好等）
└── 项目专属文件    ← 如 PROJECT.md / PROGRESS.md（如果有的话）
```

**`AGENTS.md` 存的是操作规则（SOP）**，不是动态内容。比如：
- "关机时必须写 summary"
- "重启前备份 memory 文件夹"

这些**规则**才放在 `AGENTS.md`。

---

## 关机流程的写入目标

```
备份 → 写 summary → 更新项目进度 → 写日志 → 清会话 → 汇报
         ↓                ↓              ↓
      MEMORY.md       MEMORY.md      日志文件（如 LOG.md）
      或项目文件      或项目文件      （非7核心文件之一）
```

---

## 建议

如果你希望**汇报内容**在下次开机时被读取，应该：

1. **Summary / 进度** → 写进 `MEMORY.md`（长期记忆）
2. **操作规则更新**（如发现新问题要加检查项）→ 更新 `AGENTS.md`
3. **用户偏好变化**（如发现你喜欢某种汇报格式）→ 更新 `USER.md`

---

所以：**汇报本身不保存在 AGENTS.md，AGENTS.md 只保存"汇报的规则"**。汇报内容走 `MEMORY.md` 或独立日志文件。

这个区分对吗？还是你想把汇报也塞进 `AGENTS.md` 里？

---

### 🧑 用戶
*2026-05-05T04:41:19.567096Z*

openclaw-startup.sh "开机""开始工作" 。 封存旧会话 → 清理工作台 → 读进度汇报 
openclaw-shutdown.sh | "关机"  "下线" / "重启电脑" | 备份 → 写 summary → 更新项目进度 → 写日志 → 清会话 → 汇报保存在AGENTS.md对吗？

---

### 🤖 Kimi
*2026-05-05T04:32:48.130Z*

这个仓库是 OpenClaw 的 Agents 项目，我来搜索一下这些文件的实际内容和项目背景。

根据搜索到的官方和社区文档，给你整理 OpenClaw 这 7 个文件的作用和优先级。

---

## 各文件作用

| 文件 | 作用 | 加载时机 |
|------|------|--------|
| **AGENTS.md** | 操作指令 + 行为准则。定义"怎么做事"：会话启动顺序、记忆协议、安全规则、路由策略、哪些操作需要确认。相当于员工的 SOP 手册。[^3^] [^4^] [^8^] | 每次会话启动 |
| **SOUL.md** | 灵魂/人格内核。定义性格、语气、幽默感、沟通边界、三观、底线（Never 列表）。不可被后续对话修改。[^1^] [^4^] [^11^] | 每次会话启动 |
| **IDENTITY.md** | 身份名片。名字、头像、emoji、角色概念。3-5 行极简，不承载性格深度。[^4^] [^11^] | 每次会话启动 |
| **USER.md** | 用户画像。你是谁、怎么称呼你、偏好、OS、时区、沟通风格。[^4^] [^7^] | 每次会话启动 |
| **MEMORY.md** | 长期记忆。Agent 学习到的模式、项目历史、提炼的偏好。需要定期整理，保持精简。[^4^] [^8^] | 仅主会话 |
| **TOOLS.md** | 工具环境速查。API 地址、SSH 别名、设备名、端口。不控制工具开关，只记"东西在哪"。[^4^] [^8^] | 每次会话启动 |
| **HEARTBEAT.md** | 心跳任务清单。定时检查项（如每 30 分钟扫一遍收件箱、任务状态）。保持简短， checklist 形式。[^1^] [^4^] | 心跳运行时 |

---

## 读取优先级排序（从高到低）

```
1. SOUL.md        ← 人格内核，最高原则，不可覆盖
2. AGENTS.md      ← 操作规则，行为边界，安全策略
3. IDENTITY.md    ← 身份标识，快速定位"我是谁"
4. USER.md        ← 用户画像，个性化沟通基础
5. TOOLS.md       ← 工具环境，执行层参考
6. MEMORY.md      ← 长期记忆，动态学习层
7. HEARTBEAT.md   ← 定时任务，仅在心跳触发时读取
```

**排序逻辑：**

- **SOUL** 在最顶层，定义 Agent 的"宪法"和底线，任何情况下不可违背。
- **AGENTS** 次之，是行为规则层，告诉 Agent 能做什么、不能做什么、怎么做。
- **IDENTITY** 第三，是身份锚点，Agent 一开口就知道自己是谁。
- **USER** 第四，了解服务对象，个性化响应。
- **TOOLS** 第五，执行时的环境参考，受上层规则约束。
- **MEMORY** 第六，动态积累的知识，需要定期整理，不是刚性规则。
- **HEARTBEAT** 最末，只在定时触发时读取，不参与每次对话决策。

---

> ⚠️ **重要**：这 7 个文件**每次会话都会注入到系统提示词**中，会消耗 Token。文件越精简越好，尤其是 `MEMORY.md` 会随时间膨胀，需要定期清理。[^8^]

---

### 🧑 用戶
*2026-05-05T04:32:48.129994Z*

openclaw的这个几个文件分别是什么作用，读取的优先度。。排序

---

### 🤖 Kimi
*2026-05-05T04:30:58.830806Z*

先简单看一下这几个文件的内容，再给你解释和排序。

直接读本地文件吧。

文件不在 upload 目录，我看看实际有哪些文件。

图片里只有文件名，没有实际文件内容。我根据这些文件名在 AI Agent 系统上下文中的常见含义来解释，并给出优先级排序。

---

## 各文件作用

| 文件 | 作用 |
|------|------|
| **IDENTITY.md** | 定义 Agent 的身份、角色、性格、说话风格。你是谁、怎么说话、什么态度。 |
| **SOUL.md** | 核心灵魂/价值观层。Agent 的信念、原则、不可违背的底线、道德框架。 |
| **MEMORY.md** | 记忆管理。短期/长期记忆的存取规则、记忆空间的使用规范。 |
| **AGENTS.md** | 多 Agent 协作配置。如果有多个 Agent 分工，这里定义各自职责和协作流程。 |
| **TOOLS.md** | 工具调用规范。可用工具列表、调用规则、参数格式、错误处理。 |
| **HEARTBEAT.md** | 健康检查/自检机制。Agent 定期自我检查状态、资源使用、异常恢复。 |
| **USER.md** | 用户画像。用户的偏好、历史交互、个性化设置。 |

---

## 权限优先级排序（从高到低）

```
1. SOUL.md        ← 最高：价值观和底线，任何情况下不可覆盖
2. IDENTITY.md    ← 身份定义，决定行为基调和角色边界
3. MEMORY.md      ← 记忆规则，影响上下文连贯性
4. USER.md        ← 用户偏好，个性化层
5. TOOLS.md       ← 工具使用规范，执行层
6. AGENTS.md      ← 多 Agent 协作（单 Agent 时权重降低）
7. HEARTBEAT.md   ← 最低：运行监控，不影响核心逻辑
```

**排序逻辑：**

- **SOUL** 在最顶层，因为它定义了"什么绝对不能做"，是安全护栏。
- **IDENTITY** 次之，它决定 Agent 的基本行为模式和角色边界。
- **MEMORY** 第三，没有记忆就没有上下文，Agent 会变成"金鱼"。
- **USER** 第四，用户偏好很重要，但不能违背前三个层级的约束。
- **TOOLS** 第五，是执行手段，受上层规则约束。
- **AGENTS** 第六，只在多 Agent 场景生效。
- **HEARTBEAT** 最末，纯运维监控，不影响决策逻辑。

---

这个排序合理吗？还是你想调整某个层级的位置？

---

### 🧑 用戶
*2026-05-05T04:30:58.830800Z*

这几个文件的作用解释一个然后在把权限优先的先后排序。

---

