# Session: 2026-05-20 03:30:19 GMT+8

- **Session Key**: agent:main:main
- **Session ID**: 5bfa2b2c-245c-46f9-830a-27a3540bdc8a
- **Source**: webchat

## Conversation Summary

user: [Wed 2026-05-20 03:23 GMT+8] Chrome我没有这个
assistant: 这是 OpenHuman 内嵌浏览器（CEF）的安全拦截，不是你系统的 Chrome。

**在 OpenHuman 里操作：**

1. 看当前页面有没有弹出 **允许/Allow** 按钮，点允许
2. 如果没有弹窗，检查 OpenHuman 设置里有没有 **"Network"** 或 **"Security"** 选项，关掉本地网络拦截
3. 或者在 OpenHuman 启动时加参数（从命令行启动）：
```powershell
"C:\Program Files\OpenHuman\OpenHuman.exe" --disable-web-security
```

关了安全限制临时用可以，正式用还是要靠它订阅通道。你先把 LM Studio 跑起来（端口 1234 有响应），然后重启 OpenHuman 看能不能过这关。
user: [Wed 2026-05-20 03:23 GMT+8] 你帮我设置是不是用的wsl的哟
assistant: 不是。LM Studio 是 Windows 原生应用，模型文件在 `C:\Users\lz\.lmstudio\models\`，Windows 自己的文件系统。我只是通过 WSL2 的文件路径 `/mnt/c/...` 帮你复制过去的。

验证一下：Windows 文件管理器打开 `C:\Users\lz\.lmstudio\models\` 看看能不能看到 `qwen2.5-7b-Q5_K_M.gguf`。另外 LM Studio 里服务器跑起来了吗？端口 1234 有响应才算数。
assistant: 端口 1234 还没响应，LM Studio 服务器没跑起来。

你在 LM Studio 里：
1. 先在模型列表找到 `qwen2.5-7b`
2. 点 **加载/Start Server**
3. 确认它显示 **Server running on port 1234**

跑起来后 OpenHuman 才能连上。
user: [Wed 2026-05-20 03:24 GMT+8] 我是openhuman
assistant: 对，就是你截图里那个 OpenHuman 欢迎页面。

这个安全提示是 OpenHuman 内嵌浏览器拦了自己连本地 `localhost:1234`。

你先确认 LM Studio 那边服务器跑起来了没？跑起来后回到 OpenHuman 这个页面，看有没有**"允许"按钮**可以点。

如果之前已经登录过，看能不能跳过欢迎页直接进设置：左上角菜单 → **Settings → AI & Skills → Local AI**，把 `local_ai.provider` 改成 `lm_studio`，`local_ai.runtime_enabled` 打开。
user: [Wed 2026-05-20 03:24 GMT+8] 你跟我扯鸡巴呀
assistant: 对不起。

老实说——你现在卡的不是网络权限的问题。OpenHuman Early Beta 没有订阅就是进不去，不管你本地模型跑不跑、端口通不通。它的核心聊天功能锁死走订阅通道，本地 AI 只是辅助。

这个安全提示只是个表象，本质是：**它还没开放使用**。要么等它发正式版，要么死心。
user: [Wed 2026-05-20 03:25 GMT+8] 跳至主要内容
Chrome for Developers
更多
搜索
/


中文 – 简体
登录
Blog

Google uses AI technology to translate content into your preferred language. AI translations can contain errors.
Switch to English
Chrome for Developers
Blog
该内容对您有帮助吗？

新增了本地网络访问权限提示




Chris Thompson
Chris Thompson

发布时间：2025 年 6 月 9 日

更新（2025 年 9 月 29 日）： 本地网络访问权限提示将在 Chrome 142 中推出。 我们撰写了一份 LNA 采用指南，解答了常见问题。
Chrome 正在为根据本地网络访问规范连接到用户本地网络的网站添加新的权限提示。 此举旨在保护用户免遭针对专用网络上的路由器和其他设备的跨站请求伪造 (CSRF) 攻击，并降低网站利用这些请求对用户本地网络进行指纹识别的能力。

为了解此变更对 Web 生态系统的影响，Chrome 团队正在征求开发者的反馈意见。这些开发者构建的 Web 应用依赖于与用户本地网络或用户机器上本地运行的软件建立连接。从 Chrome 138 开始，您可以前往 chrome://flags/#local-network-access-check 并将相应标志设置为“已启用（阻止）”，选择启用这些新限制。

注意： Chrome 之前曾尝试通过专用网络访问来限制对本地网络设备的访问，这需要 CORS 预检，其中目标设备选择允许连接，而不是通过权限提示来限制所有本地网络访问。在 PNA 暂停后，本地网络访问权限取代了这项工作。感谢所有帮助测试 PNA 并向我们提供反馈的开发者，这些反馈非常有价值，有助于我们确定新的本地网络访问权限方法。
什么是本地网络访问权限？
本地网络访问权限会限制网站向用户本地网络中的服务器（包括在用户机器上本地运行的服务器）发送请求的能力，要求用户先向网站授予权限，然后才能发出此类请求。请求此权限的能力仅限于安全上下文。

显示“查找并连接到本地网络上的任何设备”文本的提示。
Chrome 的本地网络访问权限提示示例。
许多其他平台（例如 Android、iOS 和 MacOS）都具有本地网络访问权限。例如，您可能在设置新的 Google TV 和 Chromecast 设备时，向 Google Home 应用授予了此权限，以允许其访问本地网络。

哪些类型的请求会受到影响？
在本地网络访问权限的第一个里程碑中，我们认为“本地网络请求”是指从公共网络到本地网络或环回目的地的任何请求。

本地网络是指解析为预留供本地使用的地址的任何目的地。例如，RFC1918 第 3 部分中指定的专用 IPv4 地址（例如，192.168.0.0/16）、RFC3927 中定义的 IPv4“链路本地”前缀 (169.254.0.0/16)、RFC4193 第 3 节中定义的 IPv6“唯一本地地址”前缀 (fc00::/7)、RFC4291 第 2.5.6 节中定义的 IPv6“链路本地”前缀 (fe80::/10)，或者映射的 IPv4 地址本身是本地的 IPv4 映射 IPv6 地址 (::ffff:0:0/96)。

环回是指解析为本地机器的任何目的地（即 “环回”接口），例如 RFC1122 第 3.2.1.3 节中定义的 IPv4 环回前缀 (127.0.0.0/8)，或 RFC4291 第 2.5.3 节中定义的 IPv6 环回 (::1/128)。

如需查看 IP 地址与地址空间的完整映射，请参阅本地网络访问规范中的表格。

公共网络是指任何其他目的地。

注意： 未来，我们计划扩展这些保护措施，以涵盖所有发送到本地网络上目的地的跨源请求。这包括从本地服务器（例如 https://router.local）到本地网络上其他服务器的请求，或从本地服务器到本地主机的请求。
由于本地网络访问权限仅限于安全上下文，并且将本地网络设备迁移到 HTTPS 可能很困难，因此，如果 Chrome 在解析目的地之前知道请求将发送到本地网络，则受权限限制的本地网络请求现在将免于混合内容检查。如果满足以下条件，Chrome 便知道请求将发送到本地网络：

请求主机名是专用 IP 字面值（例如 192.168.0.1）。
请求主机名是 .local 网域。
fetch() 调用带有 targetAddressSpace: "local". 选项的注释
// Example 1: Private IP literal is exempt from mixed content.
fetch("http://192.168.0.1/ping");

// Example 2: `.local` domain is exempt from mixed content.
fetch("http://router.local/ping");

// Example 3: Public domain is not exempt from mixed content,
// even if it resolves to a local network address.
fetch("http://example.com/ping");

// Example 4: Adding the `targetAddressSpace` option flags that
// the request will go to the local network, and is thus exempt
// from mixed content.
fetch("http://example.com/ping", {
 targetAddressSpace: "local",
});
Chrome 中的变化
Chrome 138
我们已在 Chrome 138 中推出本地网络访问权限的初始版本，供您选择加入并进行测试。用户可以将 chrome://flags#local-network-access-check 设置为“已启用（阻塞）”，以启用新的权限提示。这支持为使用 JavaScript fetch() API、子资源加载和子框架导航发起的请求触发本地网络访问权限提示。

我们提供了一个演示网站，网址为 https://lna-testing.notyetsecure.com/，用于触发不同形式的本地网络请求。

已知问题和限制
与本地网络的 WebSocket (crbug.com/421156866)、WebTransport (crbug.com/421216834) 和 WebRTC (crbug.com/421223919) 连接尚未受 LNA 权限的限制。
来自 Service Worker 和 Shared Worker 的本地网络请求要求相应 Worker 的来源之前已获得本地网络访问权限。
如果您的应用从服务工作线程发出本地网络请求，您需要从应用中单独触发本地网络请求，才能触发权限提示。（我们正在努力开发一种方法，让工作者在有有效文档可用时触发权限提示 - 请参阅 crbug.com/404887282。）
Chrome 139 及更高版本
我们的目标是尽快发布本地网络访问权限。考虑到某些网站可能需要额外的时间来更新本地网络访问权限注释，我们将在默认提供本地网络访问权限之前添加一项源试用，让网站暂时选择不使用安全上下文要求。这应能为开发者提供更清晰的迁移途径，尤其是当您依赖于通过 HTTP 访问本地网络资源时（因为如果从尚不支持本地网络访问混合内容豁免的浏览器中的 HTTPS 页面请求这些资源，这些请求会被阻止，因为它们属于混合内容）。

我们还将添加一项 Chrome 企业政策，用于控制哪些网站可以发出本地网络请求，哪些网站不可以（预先向这些网站授予或拒绝相应权限）。这样一来，受管理的 Chrome 安装（例如公司环境中的 Chrome 安装）就可以避免针对已知的预期使用情形显示警告，或者进一步锁定并阻止网站请求该权限。

我们计划继续将“本地网络访问权限”与可向本地网络发送请求的不同功能相集成。例如，我们计划很快推出适用于 WebSocket、WebTransport 和 WebRTC 连接的本地网络访问权限。

随着我们越来越接近在 Chrome 中全面推出本地网络访问功能，我们将分享更多相关信息。

欢迎提供反馈
之前有关专用网络访问权限开发的反馈对我们非常有价值，引导我们采用了新的本地网络访问权限方法。我们要再次感谢多年来参与其中的所有人。

如果您开发或使用依赖于与用户本地网络或用户机器上本地运行的软件建立连接的网站，Chrome 团队希望了解您的反馈和使用情形。您可以采取以下两种做法来提供帮助：

前往 chrome://flags#local-network-access-check，将该标志设置为“已启用（阻塞）”，然后查看您的网站是否正确触发了新的权限提示（并在您授予权限后按预期运行）。
如果您遇到任何问题或有任何反馈，请在 Chromium 问题跟踪器中提交问题，或在我们的 LNA 规范 GitHub 代码库中提交问题。 Chrome 非常期待收到您的反馈。
该内容对您有帮助吗？

如未另行说明，那么本页面中的内容已根据知识共享署名 4.0 许可获得了许可，并且代码示例已根据 Apache 2.0 许可获得了许可。有关详情，请参阅 Google 开发者网站政策。Java 是 Oracle 和/或其关联公司的注册商标。

最后更新时间 (UTC)：2025-06-09。

参与
提交 bug
查看未处理完毕的问题
相关内容
Chromium 更新
案例研究
归档
播客和节目
关注
关于 X 的 @ChromiumDev
YouTube
LinkedIn 上的 Chrome for Developers
RSS
条款
隐私权政策

中文 – 简体
assistant: 这是 Chrome 142 的新安全策略——**本地网络访问权限（LNA）**，会自动拦网站连 localhost。

OpenHuman 内嵌的 CEF 浏览器也受这个策略影响，所以它连不上你的 LM Studio（`localhost:1234`）。

**解决办法：**

1. 系统 Chrome 打开 `chrome://flags/#local-network-access-check`，设成 **Disabled**，重启 Chrome。CEF 有时也读系统 Chrome 的配置。
2. 如果不行，**OpenHuman 启动参数加白名单**：
```powershell
"C:\Program Files\OpenHuman\OpenHuman.exe" --disable-web-security --ignore-certificate-errors --allow-running-insecure-content
```
3. 还不行的话，把 LM Studio 改成监听 `0.0.0.0:1234`（而不是 `127.0.0.1`），OpenHuman 用你电脑的局域网 IP（如 `192.168.8.x:1234`）去连，绕过 localhost 限制。
user: [Wed 2026-05-20 03:26 GMT+8] 来源
http://127.0.0.1:7788
在“网络”面板中查看请求
安全
此来源是非 HTTPS 安全来源。
assistant: 这是 **OpenHuman 自己的核心进程**，不是 LM Studio。`127.0.0.1:7788` 是 OpenHuman 前端连自己 Rust 后端的端口。

说明 OpenHuman 的 Rust 核心（`openhuman-core.exe`）没跑起来或者崩了。这是它 Early Beta 的 bug，跟你装模型、LM Studio 都没关系。

**试试：**
1. 任务管理器看有没有 `openhuman-core.exe` 进程
2. 没有的话，用管理员权限启动 OpenHuman
3. 或者从命令行启动看报什么错：
```powershell
cd "C:\Program Files\OpenHuman"
.\OpenHuman.exe --verbose
```

如果核心进程起不来，那就是 Windows 版兼容性问题，只能等更新。
user: [Wed 2026-05-20 03:29 GMT+8] 偏好设置

工作区

实验

忽略列表

设备

节流

位置

快捷键

持久性

□启用本地替换

调试程序

停用JavaScript

停用异步堆栈轨迹

全局

为弹出式窗口自动打开开发者工具

即输即搜

itte

账号

您尚未登录 Chrome。

. auu Dasic eval (700]

恢复默认值并重新加裁

4 TTTOTTtTIS ago
assistant: 这是 Chrome DevTools 设置页，不是 OpenHuman 的。你现在在浏览器里打开了开发者工具。

回到 OpenHuman 窗口，看核心进程跑没跑：任务管理器搜 `openhuman-core.exe`。
