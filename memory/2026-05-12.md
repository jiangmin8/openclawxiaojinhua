# Memory — 2026-05-12

## Quick Reference
> "手搓" means building from scratch, never installing ready-made solutions.
> The user ("大哥") wants simple, direct interfaces — no bloat.

## Environment
- WSL2 (Ubuntu 22.04), OpenClaw Gateway on 18789
- Default model: deepseek-v4-pro (set 15:03)
- Scripts at: ~/scripts/switch-model.sh [flash|pro|llama]
- oc alias in ~/.bashrc: status|start|restart|stop|log (no switch branch)

## Chrome Installation
- Installed at: `/home/lz/.local/share/chrome-linux64/chrome` (Chrome 148, ~176MB)
- LD_LIBRARY_PATH: `/home/lz/.local/share/nss-libs`
- Downloaded via Tsinghua mirror (npmmirror.com) — fast in China
- Used for Puppeteer-based web scraping
- **OpenClaw browser tool does NOT detect this Chrome** (shows "unknown" / "auto" detection).
  - Can NOT use OpenClaw's built-in `browser` tool without proper pairing or PATH detection.
  - Workaround: use Puppeteer directly with executablePath.

## Custom Panel: Claw Panel (龙虾面板)
- Path: `/home/lz/claw-panel/`
- Server: Node.js static server + API proxy on port **3333**
- Features: Dashboard overview, Agent list, Chat, Logs, Cron jobs
- API proxy: all /api/* routes forwarded to Gateway at 127.0.0.1:18789
- Designed as tabbed layout (simple, no bloat per user request)
- Current version: v1 (basic), user wants minimal/simple UI

## Theme
- Custom dark theme "Athena Night" created at:
  `/home/lz/.local/share/pnpm/store/v11/links/@/openclaw/2026.5.7/.../dist/control-ui/athena-theme.json`
- Import via Control UI → Appearance → paste URL: `http://127.0.0.1:18789/athena-theme.json`
- Warm dark tones (oklch(0.14 background), warm amber primary)

## Desktop Cleanup (Done)
- /mnt/c/Users/lz/Desktop/ cleaned from 44 → 11 items
- Keep: `核心文件不能删除.txt`, `定版/`, `工作草稿/`, `OpenClaw项目/`, `桌面/`, 7 shortcuts

## Cron Tasks (Configured)
1. Hourly healthcheck (Llama model, id: e219cb27-...)
2. Daily 22:00 backup check (Llama model, id: fcda9fd3-...)

## Known Issues
- OpenClaw browser tool can't use the installed Chrome (detection mismatch)
- Douyin videos accessible via Puppeteer but may require retry
- Gateway restart from panel API needs proper endpoint discovery
