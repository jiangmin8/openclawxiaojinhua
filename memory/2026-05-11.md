# Memory 2026-05-11

## Session Summary
Long troubleshooting session covering Gateway config, WiFi repair, C:\ cleanup, QQ Bot setup, and router diagnostics.

---

## Gateway & Config
- **Gateway port**: 18789 (loopback), managed by systemd user service `openclaw-gateway.service`
- **Auto-start**: `systemctl --user enable openclaw-gateway.service` — starts on WSL login
- **Current PID**: 56260
- **DeepSeek model**: `deepseek-v4-pro`, `supportsTools: false` (DSML format incompatible with OpenClaw)
- **NVIDIA provider**: Added to config — `baseUrl: https://integrate.api.nvidia.com/v1`, model `meta/llama-3.3-70b-instruct`, `supportsTools: true`
- **⚠️ Both DeepSeek and NVIDIA API keys were exposed in chat — user needs to rotate both keys**
- **Switching models**: Change `agents.defaults.model.primary` between `deepseek-v4-pro` and `meta/llama-3.3-70b-instruct`

## QQ Bot
- **Plugin**: `@tencent-connect/openclaw-qqbot` installed
- **AppID**: 1903976111
- **Config path**: `channels.qqbot` in openclaw.json
- **Note**: `openclaw channels add --channel qqbot` DOES NOT WORK — must edit JSON directly. Plugin also requires `plugins.entries.qqbot.enabled: true`
- **Status**: Connected and working

## Router: GL-MT3600BE
- **Firmware**: 4.8.7 (GL.iNet official)
- **WiFi driver**: MT7993, known bug in 4.8.7
- **2.4GHz bug**: `nobcn occurs` / `send_beacon_fail` — driver wdev corruption on wifi reload, beacon thread dead
- **Root cause (from pstore)**: `APStopByBss() Invalid wdev(0)` crash on shutdown, leaves 2.4GHz radio in broken state
- **Fix**: `wifi down && wifi up` (full driver reload, not just reload)
- **SSID**: `GL` (2.4GHz), `GL5G` (5GHz)
- **SSD**: 112GB ext4 at `/mnt/sda1`, 101GB free — was previously unmounted
- **Backups**: `openclaw_backup` (184MB) + `openclaw_backup.20260502_0310` (184MB) on SSD
- **AdGuardHome**: Installed on router
- **Newer firmware available**: 4.8.8, 4.8.10, 4.9.0 (need Cloudflare bypass to download)
- **ROM**: 64M/64M used (100% full — tight)
- **IP**: 192.168.8.1, SSH key at `~/.ssh/router_new`

## Windows C:\ Cleanup
- **Before**: 189G used / 44G free (82%)
- **After**: 156G used / 78G free (67%) — freed 33GB
- **Biggest culprit**: `Temp/wsl-crashes` (32GB WSL crash dumps)
- **Also cleaned**: Win update temp, Edge cache, diagnostics, recycle bin
- **Kept**: SodaMusic, QQ cache (user to decide later)
- **Warning**: `hiberfil.sys` (13.7GB) and `pagefile.sys` (12.2GB) remain

## DeepSeek V4 Pro Tool Calling Issue
- V4 Pro outputs `<invoke>` DSML tags instead of standard OpenAI function_call JSON
- OpenClaw parser can't understand this dialect → outputs raw tags + `[assistant turn failed before producing content]`
- **Workaround**: `supportsTools: false` → pure text mode, user runs commands manually
- **Permanent fix**: Use NVIDIA-hosted models which enforce standard OpenAI format

## User Preferences
- Prefers concise, no-fluff responses
- QQ is primary mobile channel
- Router WiFi stability is critical
- Will upgrade MT3600BE firmware when convenient
- C:\ cleanup requires approval before deletion
